import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const userSlice = createSlice({
    name: 'post',
    initialState,
    reducers: {

    }
})

export default userSlice.reducer;